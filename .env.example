# Copy to .env and adjust as needed.
APP_ENVIRONMENT=development
APP_LOG_LEVEL=INFO
DEEPSEEK_API_KEY=
# JWT 签名密钥（开发可用示例值；生产必须替换，建议用 openssl rand -hex 32 生成）
JWT_SECRET_KEY=dev-only-insecure-secret-change-me-32bytes
