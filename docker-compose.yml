services:
  api:
    build: .
    image: ai-demo-api:latest
    container_name: ai-demo-api
    ports:
      - "8000:8000"
    environment:
      APP_ENVIRONMENT: production
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
    restart: unless-stopped
